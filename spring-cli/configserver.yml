spring:
  profiles:
    active: git
  cloud:
    config:
      server:
        git:
          uri: https://github.com/marzelwidmer/feature-toggle-demo.git
          strict-host-key-checking: false
          search-paths: config-repo

